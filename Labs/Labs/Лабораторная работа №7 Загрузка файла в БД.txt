Тип данных для столбца хранения файлов в БД: varbinary(MAX)

Для импорта файла в БД нужно разработать клиентское приложение:
Загрузка файла в БД в клиентском приложении:

byte[] fileData = File.ReadAllBytes(полное имя файла); // чтение содержимого файла
if (fileData.Length < 1024) // проверка, что размер меньше 1 КБ
{
    // выполнение SqlCommand с параметром, в параметр передать значение fileData
}